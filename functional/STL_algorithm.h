#pragma once

#include "find.h"
#include "count.h"
#include "range_algo.h"
#include "iterator_algo.h"

using stl_find = stl_algorithm::find;
using stl_find_if = stl_algorithm::find_if;
using stl_find_if_not = stl_algorithm::find_if_not;
using stl_find_first_of = stl_algorithm::find_first_of;
using stl_adject_find = stl_algorithm::adject_find;
using stl_count = stl_algorithm::count;
using stl_count_if = stl_algorithm::count_if;
using stl_accumulate = stl_algorithm::accumulate;
using stl_mismatch = stl_algorithm::mismatch;
using stl_equal = stl_algorithm::equal;
using stl_advance = stl_algorithm::advance;
using stl_distance = stl_algorithm::distance;
using stl_is_permuatation = stl_algorithm::is_permutation;
using stl_search = stl_algorithm::search;
using stl_min_element = stl_algorithm::min_element;
using stl_min_element = stl_algorithm::min_element;
using stl_adj_difference = stl_algorithm::adjacent_difference;
using stl_inner_product = stl_algorithm::inner_product;
using stl_partial_sum = stl_algorithm::partial_sum;